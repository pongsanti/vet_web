import { createAction } from 'redux-actions';

export const noti_add = createAction('NOTI_ADD');
export const noti_clear = createAction('NOTI_CLEAR');
export const noti_reset = createAction('NOTI_RESET');
